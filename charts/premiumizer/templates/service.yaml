apiVersion: v1
kind: Service
metadata:
  name: {{ include "premiumizer.fullname" . }}
  labels:
    {{- include "premiumizer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "premiumizer.selectorLabels" . | nindent 4 }}
