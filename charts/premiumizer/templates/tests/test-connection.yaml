apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "premiumizer.fullname" . }}-test-connection"
  labels:
    {{- include "premiumizer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "premiumizer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
